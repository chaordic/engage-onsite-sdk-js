version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.14-browsers
    steps:
      - checkout
      - run:
          name: Yarn
          command: yarn
      - run:
          name: Test
          command: npx run test

